name: Release-Erstellung und Asset-Upload

on:
  push:
    tags:
      - '*'

jobs:
  build_and_release:
    runs-on: ubuntu-latest
    permissions:
      contents: write 
    steps:
      - name: ⬇️ Repository auschecken
        uses: actions/checkout@v4
  
      - name: 🏗️ Node.js Dependencies und Build
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm install
      - run: npm run build 

      - name: 🧱 Binärdateien als ZIP-Datei verpacken
        run: |
          
          zip -r memory-card-game-binaries.zip dist/
          echo "ZIP-Datei erstellt: memory-card-game-binaries.zip"
      
      - name: 📤 GitHub Release erstellen und Assets hochladen
        uses: softprops/action-gh-release@v2
        with:
          name: Release ${{ github.ref_name }}
          body: |
          files: memory-card-game-binaries.zip
  
